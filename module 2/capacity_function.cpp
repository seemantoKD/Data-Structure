#include <bits/stdc++.h>
using namespace std;
int main()
{
    /*
    // output dekhte hobe bujte hole
    vector<int> v;
    cout << v.capacity() << endl;

    v.push_back(50);
    cout << v.capacity() << endl;

    v.push_back(500);
    cout << v.capacity() << endl;

    v.push_back(20);
    cout << v.capacity() << endl;

    v.push_back(40);
    cout << v.capacity() << endl;

    v.push_back(60);
    cout << v.capacity() << endl;
    */

    /*
    vector<int> v = {10,20,30};
    cout<<v.size()<<endl;

    v.clear();
    cout<<v.size()<<endl; //size -> 0

    //clear korar por value roye jay tar proman
    cout<<v[2]<<endl;
    */

    /*
    vector<int> v = {10, 20, 30}; //initial size -> 3
    cout << "Primary vector -> ";
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << " ";
    }
    cout<<endl;

    // v.resize(2); //after resizing : size -> 2
    // cout << "after resize vector -> ";
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    v.resize(7,100); //again resizing and size update by -> 7
    cout << "after resize vector -> ";
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << " ";
    }
        */
}