#include <bits/stdc++.h>
using namespace std;
int main()
{
    /*
    //way 1
    list<int> l;
    cout << l.size() << endl;
    */

    /*
    // way 2 with size
    list<int> l(10);
    cout << l.size() << endl;
    */

    /*
    // way 3 with sizes and values
    list<int> l(10, 5);
    // cout << *l.begin() << endl;
    for (int val : l)
    {
        cout << val << endl;
    }
    */

    /*
    // way 4 one list initialize with another list
    list<int> l1 = {1, 2, 3, 4, 5};
    list<int> l2(l1);
    for (int val : l2)
    {
        cout << val << endl;
    }
    */

    /*
    // way 5
    // int arr[] = {10, 20, 30};
    // list<int> l(arr, arr + 3);
    vector<int> v = {1, 2, 3};
    list<int> l2(v.begin(), v.end());
    for (int val : l2)
    {
        cout << val << endl;
    }
    */
    return 0;
}