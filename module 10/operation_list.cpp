#include <bits/stdc++.h>
using namespace std;
int main()
{
    /*
    list<int> l = {20, 20, 50, 60, 40, 10};
    // usses of remove function
    l.remove(60);
    for (int val : l)
    {
        cout << val << endl;
    }
    */

    /*
    list<int> l = {20, 20, 50, 60, 40, 10};
    // usses of sort function
    l.sort();               // sort list in ascending order
    l.sort(greater<int>()); // sort  list in descending order
    for (int val : l)
    {
        cout << val << endl;
    }
    */

    /*
    list<int> l = {20, 20, 50, 60, 40, 10};
    // usses of unique function
    l.sort();
    l.unique();
    for (int val : l)
    {
        cout << val << endl;
    }
    */


    list<int> l = {20, 20, 50, 60, 40, 10};
    // usses of reverse function
    l.reverse();
    for (int val : l)
    {
        cout << val << endl;
    }
    return 0;
}