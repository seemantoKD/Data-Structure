#include <bits/stdc++.h>
using namespace std;
int main()
{
    /*
    list<int> l = {10, 20, 50, 30, 50};
    cout << l.back() << endl;  // usses of back function
    cout << l.front() << endl; // usses of front function
    */

    /*
    list<int> l = {10, 20, 50, 30, 50};
    cout << *next(l.begin(), 2) << endl; // usses of next function that access ith element on the linked list
    */

    /*
    //list input and output
    int n;
    cin >> n;
    list<int> l(n);
    for (int i = 0; i < n; i++)
    {
        cin >> *next(l.begin(), i);
    }

    for (int i = 0; i < n; i++)
    {
        cout << *next(l.begin(), i) << endl;
    }
    */

    
    return 0;
}